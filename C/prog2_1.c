#include <stdio.h>
#include <stdlib.h>

int main(void) 
{
    int num;
    num = 2;
    printf("%d + %d = %d \n", num, num, num+num);
    printf("see u tomorrow \n");
    printf("you have %d cats, too. \n", num);
    /* system("pause"); */
    
    return 0;
}